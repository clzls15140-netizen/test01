#include <stdio.h>

int main()
{
    char name[100];
    int length = 0; 

    printf("입력\n");
    scanf("%s", name); 

    while (name[length] != '\0')
    {
        length++; 
    }

    printf("\n출력\n");
    printf("문자열길이:%d\n", length);

    return 0;
}

